{
    "id": "string",           // 图的唯一ID（与数据库主键一致）
    "name": "string",         // 图的名称
    "desc": "string",         // 图的描述（可选）
    "nodes": [
      {
        "id": "string",           // 节点唯一ID
        "label": "string",        // 节点名称
        "desc": "string",         // 节点描述（可选）
        "inputFields": [
          {
            "id": "string",               // 字段唯一ID
            "name": "string",             // 字段名
            "dependendFieldIds": [        // 依赖的输入字段ID数组
              "string"
            ],
            "logic": "string"             // 字段的产出逻辑（markdown文本）
          }
        ],
        "outputFields": [
          {
            "id": "string",               // 字段唯一ID
            "name": "string",             // 字段名
            "dependendFieldIds": [        // 依赖的输入字段ID数组
              "string"
            ],
            "logic": "string"             // 字段的产出逻辑（markdown文本）
          }
        ],
        "preNodeIds": [ "string" ],       // 前置节点ID数组
        "nextNodeIds": [ "string" ]       // 后置节点ID数组
      }
    ]
  }